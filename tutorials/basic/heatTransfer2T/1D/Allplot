# faire tourner le fichier "analytical_comp.m" et vérifier qu'il n'y a pas de commentaire dans les données de sortie

#!/bin/sh
cd ${0%/*} || exit 1    # Run from this directory 
cd plots
python plot.py
evince *.pdf
